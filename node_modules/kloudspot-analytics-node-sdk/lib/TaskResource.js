"use strict";

const URL = require("../config/urls");
class TaskResource {
    // GET /api/v1/tasks/search
    searchTasks(callback) {
      return this.apiClient._getRequest(URL.TASK.SEARCH, callback);
    }
  }
  

  module.exports = TaskResource;
  